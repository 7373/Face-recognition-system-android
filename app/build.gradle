apply plugin: 'com.android.application'

android {
    compileSdkVersion 11
    buildToolsVersion "25.0.2"

    defaultConfig {
        applicationId "com.android.testUI"
        minSdkVersion 8
        targetSdkVersion 8
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
